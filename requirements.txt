# ChordTime V2 - Python Dependencies

# Core
numpy>=1.26.0
scipy>=1.13.0

# Audio Processing (fallback detection)
librosa>=0.10.0
soundfile>=0.12.0
mutagen>=1.47.0

# Beat/Chord Detection (fallback when ChordMiniApp unavailable)
madmom

# YouTube Download
yt-dlp[default]>=2024.0.0

# HTTP Client (for ChordMiniApp API calls)
requests>=2.31.0